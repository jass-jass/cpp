#include <iostream>
#include <string>

using namespace std;

void input(int matrix[][])
{
    int row, column;
    string str;
    cin >> str;
    for(int i=0; i++; i<(str.length())-1)
    {
        if(str[i+2] == '\n')
        {
            return;
        }
        if()
        matrix[row][column] = 
    }
}

int main()
{
    int matrix [][];
    void input(matrix);
    
    
    cout<<row <<endl;
    cout<<column;
    
    return 0;
}
